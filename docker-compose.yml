services:
  wasd-server:
    image: ghcr.io/jaemyung-0302/wasd-server:latest
    container_name: wasd-server
    restart: unless-stopped
    ports:
      - "4002:4002"
    env_file:
      - .env.wasd
    environment:
      - NODE_ENV=production
      - PORT=4002
    networks:
      - aico-network

  cooksnap-server:
    image: ghcr.io/jaemyung-0302/cooksnap-server:latest
    container_name: cooksnap-server
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - .env.cooksnap
    environment:
      - NODE_ENV=production
      - PORT=4000
      - DATABASE_URL=postgresql://cooksnap:cooksnap@cooksnap-db:5432/cooksnap
    depends_on:
      - cooksnap-db
    networks:
      - aico-network

  cooksnap-db:
    image: postgres:16-alpine
    container_name: cooksnap-db
    restart: unless-stopped
    ports:
      - "5436:5432"
    environment:
      - POSTGRES_USER=cooksnap
      - POSTGRES_PASSWORD=cooksnap
      - POSTGRES_DB=cooksnap
    volumes:
      - cooksnap-data:/var/lib/postgresql/data
    networks:
      - aico-network

networks:
  aico-network:
    driver: bridge

volumes:
  cooksnap-data:
